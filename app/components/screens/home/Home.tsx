import { FC } from 'react';
import { Text, View } from 'react-native';
import Layout from '@/components/ui/layout/Layout';

const Home: FC = () => {
	return <Layout title='Timer'></Layout>;
};

export default Home;
